# Core
Django>=5.2,<5.3
Pillow>=10.0
whitenoise>=6.6
django-modeltranslation>=0.19

# Payment Processing
stripe>=5.4

# Production server
#gunicorn>=22.0

# Optional: PostgreSQL in production (uncomment when DATABASE_URL is used)
# psycopg[binary]>=3.1
# dj-database-url>=2.1
urllib3>=2.6.3 # not directly required, pinned by Snyk to avoid a vulnerability
