gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Firebase config — project-specific, downloaded from the Firebase console
# per app/package name (see lib/push_notifications.dart and
# mobile/android/.gitignore for the same rule on the other app). Not
# committed, same as that app.
google-services.json
